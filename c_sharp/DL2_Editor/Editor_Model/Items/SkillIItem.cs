namespace Editor_Model.Items
{
    using System;
    public class SkillIItem : BaseItem
    {
        public SkillIItem(string name, int index, int size, byte[] sdgData, string sdgString) : base(name, index, size, sdgData, sdgString)
        {
        }
    }
}
